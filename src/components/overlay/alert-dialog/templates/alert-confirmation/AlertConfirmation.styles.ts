import { styled } from '@/theme';

const FlexRow = styled('div', {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  gap: '2rem',
  flexFlow: 'wrap-reverse',
});

export { FlexRow };
